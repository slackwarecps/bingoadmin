import 'package:flutter/material.dart';

class CartelaScreen extends StatelessWidget {
  const CartelaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}